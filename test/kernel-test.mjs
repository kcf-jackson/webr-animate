// import { WebR } from '@r-wasm/webr';
// import { Kernel } from '../src/kernel.js';


// // Set up WebR
// const webR = new WebR();
// await webR.init();


// // Set up Kernel
// const kernel = new Kernel(webR);

// kernel.run("2+2")
//     .then(x => x.result)
//     .then(x => x.toJs())
//     .then(console.log);

// kernel.run("list(x = 1, y = 2)")
//     .then(x => x.result)
//     .then(x => x.toObject({ depth: 0 }))
//     .then(console.log);
